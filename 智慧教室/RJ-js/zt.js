$(function () {
	// $('#contactInput').find('input').focus(function () {
	// 	$(this).parent().find('label').hide();
	// });
	// $('#contactInput').find('input').blur(function () {
	// 	if (!$(this).val()) {
	// 		$(this).parent().find('label').show();
	// 	}
	// });
});
